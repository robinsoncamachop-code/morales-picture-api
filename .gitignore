# Dependencias instaladas y datos privados generados al ejecutar la API.
node_modules/
data/*.json
data/*.tmp
.env
*.log
.DS_Store
# Respaldo portable del historial; no se añade dentro de su propio historial.
HISTORIAL_GIT.bundle
